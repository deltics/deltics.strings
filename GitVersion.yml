mode: ContinuousDeployment
increment: None
next-version: 1.2.0
branches:
  master:
    mode: ContinuousDelivery
    regex: master
    tag: 
  develop:
    regex: develop[/-]?
    tag: beta
    increment: Patch
  feature:
    regex: feature[/-]?
    tag: alpha
    increment: Minor